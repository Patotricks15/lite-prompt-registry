[package]
name = "lite-prompt-registry"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Fast in-memory prompt version governance"
license = "Apache-2.0"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
thiserror = "2"
